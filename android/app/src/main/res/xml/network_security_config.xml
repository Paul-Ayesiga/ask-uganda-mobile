<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is permitted only to the local development backend addresses
  used from the Android emulator and a developer's LAN. Production
  traffic runs over TLS and is governed by the default base config.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- Android emulator alias for the host machine. -->
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
